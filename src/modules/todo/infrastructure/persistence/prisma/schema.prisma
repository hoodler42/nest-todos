generator kysely {
  provider     = "prisma-kysely"
  output       = "../kysely/"
  fileName     = "models.kysely.d.ts"
  enumFileName = "enums.kysely.d.ts"
  camelCase    = true
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model migrations {
  id        Int    @id @default(autoincrement())
  timestamp BigInt
  name      String @db.VarChar
}

model todo {
  id           String   @id
  title        String
  is_completed Boolean  @default(false)
  created_at   DateTime @default(now())
  updated_at   DateTime @default(now())
}
