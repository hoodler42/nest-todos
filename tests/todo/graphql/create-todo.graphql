mutation createTodo($title: String!) {
    createTodo(input: { title: $title }) {
        __typename
        ... on CreateTodoSuccess {
            todo {
                id
                title
                isDone
            }
        }
        ... on InvalidTodoError {
            message
        }
    }
}
